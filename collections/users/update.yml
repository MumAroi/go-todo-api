info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/users/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "email": "test6@mail.com",
        "password": "Abc332211!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
