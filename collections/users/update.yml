info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/todos/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
          "title": "111",
          "completed": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
